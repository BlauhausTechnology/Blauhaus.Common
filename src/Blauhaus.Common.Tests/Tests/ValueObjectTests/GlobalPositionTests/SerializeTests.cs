using Blauhaus.Common.ValueObjects.Measures;
using NUnit.Framework;

namespace Blauhaus.Common.Tests.Tests.ValueObjectTests.GlobalPositionTests
{
    public class SerializeTests
    {

        //[Test]
        //public void SHOULD_serialize_and_deserialize_correctly()
        //{
        //    //Arrange
        //    var sut = GlobalPosition.Create(10.1d, 87.22d, 101d, 1055d);

        //    //Act
        //    var serialized = sut.Serialize();
        //    var result = GlobalPosition.Deserialize(serialized);

        //    //Assert
        //    Assert.That(result.Latitude, Is.EqualTo(sut.Latitude));
        //    Assert.That(result.Longitude, Is.EqualTo(sut.Longitude));
        //    Assert.That(result.Altitude, Is.EqualTo(sut.Altitude));
        //    Assert.That(result.GlobeRadius, Is.EqualTo(sut.GlobeRadius));

        //}
		
    }
}